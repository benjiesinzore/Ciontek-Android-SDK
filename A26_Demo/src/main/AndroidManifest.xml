<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--android:sharedUserId="android.uid.system"-->

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="com.mediatek.systemupdate.sysoper.permission.ACCESS_SERVICE" />

    <!-- 两种provider的权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 仅网络定位的权限 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 摄像头拍照 -->
    <uses-permission android:name="android.permission.CAMERA" />
<!--    <uses-permission  android:name="android.permission.WRITE_SETTINGS"/>-->
    <uses-feature android:name="android.hardware.camera"/>

    <!-- 读写权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<!--    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />-->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pos"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="test.apidemo.MyCrashApplication"
        >
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".IccActivity"
            android:label="@string/title_activity_icc" >
        </activity>

        <activity
            android:name=".PiccActivity"
            android:label="@string/title_activity_picc" >
        </activity>

        <activity
            android:name=".McrActivity"
            android:label="@string/title_activity_msr" >
        </activity>

        <activity
            android:name=".PciDukptActivity"
            android:label="PCI_DUKPT" >
        </activity>

        <activity
            android:name=".PrintActivity"/>
        <activity
            android:name=".SysActivity"
            android:label="@string/title_activity_others" >
        </activity>
        <activity
            android:name=".ScanActivity"
            android:label="@string/title_activity_scan" >
        </activity>
        <activity
            android:name=".EmvTestActivity"
            android:label="Emv Test"
            android:exported="true"
            >
        </activity>
        <activity
            android:name=".UpgradeOsActivity"
            android:label="@string/upgrade_os"
            android:exported="true"
            >
        </activity>

        <activity
            android:name=".PciMkSkActivity"
            android:label="PCI_MKSK" >
        </activity>

        <service
            android:name="test.apidemo.service.MyService"
            android:exported="false"
            android:permission="TODO" />

    </application>

</manifest>